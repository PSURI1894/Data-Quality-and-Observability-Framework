name: 'payments_observability'
version: '1.0.0'
config-version: 2
profile: 'default'
model-paths: ["models"]
test-paths: ["tests"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  payments_observability:
    staging:
      +materialized: view
    marts:
      +materialized: table
      +post-hook: "{{ elementary.store_elementary_test_results() }}"
